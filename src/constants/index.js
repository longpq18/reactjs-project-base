const AUTH = {
  ACCESS_TOKEN: "ACCESS_TOKEN",
};

const PAGINATION = {
  LIMIT: 10,
  PAGE: 1
}

const DATE_TIME = {
  DATE_TIME_FORMAT: "YYYY-MM-DD"
}

const LANG = {
  LANGUAGE: 'LANGUAGE',
  EN: 'EN',
  JP: 'JP'
}

export {
  AUTH,
  PAGINATION,
  DATE_TIME,
  LANG
}